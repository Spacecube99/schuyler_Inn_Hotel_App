# This is an application for the Schuyler Inn Hotel using Tailwind CSS in React Native for Expo.

to run this application use npx expo start --tunnel as it is more reliable if you are running on different networks.
